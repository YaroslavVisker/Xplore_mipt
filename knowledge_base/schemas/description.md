Purpose: Schemas that define the structure and validation rules for protocol files and other knowledge objects.

Expected contents:

- JSONSchema or Pydantic models describing:
    - protocol structure
    - criteria structure
    - code set formats
    - value definitions

- Validation rules for:
    - thresholds
    - feature requirements

DSL expressions
These schemas ensure that all protocol files are valid and consistent.